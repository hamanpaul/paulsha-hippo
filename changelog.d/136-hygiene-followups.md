---
type: feat
---
- Issue #136 fix 1c（cites frontmatter）：新增純函式 `extract_cites`（從 body 內 `path:line` 引用抽取，保序去重，上限 32 筆），`build_from_proposal` 寫入的新 note 一律帶 `cites`（無引用時為空 list，render 輸出 `cites: []`）；`_SCALAR_ORDER` 在 `source_fragments` 之後加入 `cites`。搭配 schema 修正：`parse_artifact_text` 原本只把 `tags`/`source_fragments` 特判成 bracket-list，`cites` 落到 `_parse_scalar` 回傳原始字串、round-trip 後型別壞掉；補上 `cites` 分支，元素須為 `{"path": str, "line": int}` 才接受，否則 fail closed 回退成字串，`cites: []` 正確解析為 `[]`。
- Issue #136 fix 5（follow-up ledger）：新增 `paulsha_hippo/followups.py` 與 CLI `hippo followups list|verify|close|extract`。`extract` 從 knowledge notes body 逐行抽取可行動語句（需要更新／需加／缺口／TODO 等，`ACTIONABLE_RE` 加 `re.IGNORECASE`，跳過 fenced code block）＋其 `path:line` 引用（重用 fix 1c 的 `CITE_RE`；`cites` frontmatter 僅作行內找不到時的 fallback）與預期已過時值（先排除 `CITE_RE` 命中 span 後取第一個反引號值，其次退回裸數字，空白視同無），寫入 append-only `runtime/ledger/followups.jsonl`（flush+fsync）；`fold()` 依 `(ts, original_index)` 折疊，較晚 ts 勝出；`extract_all()` 冪等（同 id 已存在即跳過）；`verify()` 唯讀重讀每筆引用的 `file:line`（±2 行，1-based，越界裁切，從不寫回被查的檔案）——expected_stale 還在→`verified-open`，不在→`resolved-in-source`（自動關閉），無法判定→`unverifiable`（附 reason：`missing-file`/`path-escape`/`no-root`/`no-expected-stale`/`line-out-of-range`）；`close()` 記手動關閉。
- 硬化：相對／絕對路徑引用皆加 `is_relative_to` 圍籬（比照 `recovery.py`/`provenance_backfill.py`），避免唯讀 `verify` 逃出 project root；目標行號遠超出檔案目前長度（±window 裁切後視窗為空）判 `unverifiable`，避免誤報 `resolved-in-source` 自動關閉；`extract_all` 落 ledger前對 `claim`／`expected_stale` 呼叫 `policy.check_boundary("external_to_raw", ...)`，fail-closed（例外即整筆跳過、`summary["skipped_redaction"]` 計數、stderr warning，不落 ledger）。
- Issue #136 fix 5 收尾：把獨立可用的 follow-up ledger 接進三個既有迴路——dream 新增可選 verify pass（janitor 之後、moc 之前，失敗絕不拉低 dream 的 clean/partial 狀態）；wakeup brief 末尾加一行 open follow-ups 計數（受 `char_budget` 約束——修正過放不下就整段丟棄，不留半行殘片、也不再超出呼叫端要求的 budget）；KPI report 每個 window 新增 open／resolved-in-source 計數。`extract` 受 `runtime_flags.followups_enabled`（config `followups.enabled`）閘門，關閉時降級 dry-run 並提示（`skipped: "followups.disabled"`），CLI 不重複判斷、閘門內建進 `extract_all`。
- 設定：`followups.enabled`（`paulsha_hippo/atomizer/atomizer.yaml` 的 `followups:` 區塊，預設 `true`）——完整新增鍵清單見 `136-hygiene-provenance.md`。
- 建議 live migration 執行順序（皆先 `--dry-run` 看候選再 `--apply`）：`hippo knowledge backfill-provenance` → `hippo knowledge mark-episodic` → `hippo knowledge link-supersedes --tier auto`（review tier 候選人工勾選後 `--accept`）→ `hippo followups extract`（放最後，因其引用 fallback 依賴 fix 1c 的 `cites`）。三支 SessionEnd hook 內容已變更（見 `136-hygiene-provenance.md`），既有安裝需重新 `hippo upgrade plan/prepare/apply`（wheel＋skill）並 `hippo install hooks`（冪等）套用，再以 `hippo doctor` 確認生效。

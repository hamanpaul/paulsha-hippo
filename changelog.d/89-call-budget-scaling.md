---
type: fix
---
- 修 issue #89：`FIXED_MAX_AGENT_CALLS=6` 未隨 #85 的 chain 時間預算縮放，導致 7+ chunk session 即使時間充足也必在第 7 次呼叫撞牆而 park。生產實證（2026-07-31T02:00Z）：大 session `claude-code:916106a1` 因 tier-1（`claude`/`codex`）宣告 `max_session_chunks: 6` 判 `ineligible: session_size`，落到 tier-2 `cg` 大 payload 品質不穩（exit 3，10.9KB 輸出、無可解析 JSON），再落到 `local-vllm`（exit 4，在 `max_tokens` 處截斷），三 profile 全滅直接 park，並重置 AR-11 soak 窗口。
- 新增 `agent_profiles.py::effective_max_agent_calls(chunk_count)`，比照 `session_deadline_seconds` 的 floor/cap 造型：`min(max(FIXED_MAX_AGENT_CALLS, chunk_count + 2), FIXED_MAX_AGENT_CALLS_CAP)`。`FIXED_MAX_AGENT_CALLS=6` 不動（≤4 chunk 行為完全不變，`4+2=6`）；新增 `FIXED_MAX_AGENT_CALLS_CAP=12`——`+2` 對齊 #86 chunk 保留契約（一次 fallback 轉移只需重補未驗證的 chunk，2 次呼叫餘裕即夠一次轉移的浪費呼叫）；cap 12 對齊 #85 的 1800s 時間 cap（1800s ÷ 240s/chunk ≈ 7.5 chunk，12 留一次 fallback 轉移的餘裕而非無界成長）。
- 縮放發生在 `run_session` 內（知道 `chunk_count` 之後），與 `session_deadline_seconds` 造型一致，但語意刻意不同：只有當 router 以**未經修改的預設值**（`self.max_agent_calls == FIXED_MAX_AGENT_CALLS`）建構時才套用縮放；呼叫端明確傳入的其他值（如 operator 收斂過的部署上限、或測試模擬的小型共享預算）維持字面值當硬上限、不縮放。這與 `deadline_seconds`（`run_session`完全不讀 `self.deadline_seconds`，任何非預設設定值即被 config 判 fail-closed）不同——一個明確傳入且異於預設的 `max_agent_calls` 仍會被真實套用（每次呼叫都受它約束），不是「宣告卻被靜默忽略」的同型缺陷，故不需要、也不應該讓 config 對它比照 `deadline_seconds` 做 fail-closed；`atomizer/config.py` 補上對應說明註解記錄兩者為何分岔。跨 profile 累計語意（issue #86 的 retention/shared-budget 契約）完全不變。
- tier-1 profile `max_session_chunks` 由 `6` 提升為 `7`（`agent_profiles.py::default_profiles()` 與出貨模板 `atomizer/atomizer.yaml` 的 `claude`／`codex` 兩處同步；理由同 cap 公式——1800s ÷ 240s/chunk ≈ 7.5，讓 7-chunk 大 session 留在品質穩定的 tier-1 而非落給 tier-2 `cg`）。**Live 部署設定不在本次變更範圍**，依本 repo「三處對齊」紀律由主線程於部署時手動同步。
- TDD：新增 `test_effective_max_agent_calls_scales_with_chunk_count_like_deadline`（純函式邊界：`chunk_count<=4→6`、`7→9`、`8→10`、`>=10→12`）與四個 router 整合測試——`test_router_session_seven_chunks_completes_without_park_under_default_call_budget`（issue 重現案：7-chunk 單一 profile 在修復前必因舊上限 6 而 park，修復後 7 次呼叫全放行）、`test_router_session_call_budget_saturates_at_cap_for_oversized_session`（13-chunk 驗證 cap 飽和於 12）、`test_router_session_explicit_call_budget_override_is_not_scaled`（明確覆寫值不被縮放覆蓋）、`test_router_session_call_budget_scaling_applies_across_fallback_transitions`（3 個 profile 各浪費一次呼叫後第 4 個 profile 接手，驗證跨 profile 累計正確吃到縮放後的 9 上限而非舊的 6）。既有 `test_router_session_shares_call_budget_across_fallback_restarts`（顯式 `max_agent_calls=3`）等既有 budget/park 測試全數維持綠燈、無需修改，僅因 tier-1 `max_session_chunks` 提升而更新 `test_default_profiles_have_max_session_chunks_bounds` 的期望值（6→7）。
- 檢查 `openspec/specs/stage2-llm-distillation/spec.md`：規範文字僅抽象描述「global deadline, maximum attempts, maximum agent calls」，未把 `6` 或任何 chunk/call 數字寫死為 normative，故本次變更不需要、也未觸碰 spec 檔。

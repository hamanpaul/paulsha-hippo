### Added

- #146：新增 `paulsha_hippo.task_memory_payload`，以最小 diff 落地 task memory payload envelope builder/validator 與 delivery outcome summarizer，固定最多三筆授權候選、shareable-safe redaction，並維持 inline/snapshot 不計入 read 的 strict KPI 語意。

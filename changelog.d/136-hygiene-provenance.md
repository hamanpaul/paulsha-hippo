---
type: feat
---
- Issue #136 fix 1a/1b（provenance 貫通）：三支 SessionEnd hook（`hooks/claude_session_end.py`／`codex_session_end.py`／`copilot_session_end.py`）在 `payload["cwd"]` 為 git repo 且原本無該鍵時 best-effort 補上 `commit`／`git_branch`／`git_dirty`（stdlib-only、2s timeout、失敗留空，每檔各自 inline 一份維持 hooks 可單檔複製部署，供下游 importer 填 provenance——目前 3218/4178 筆為 `_unknown`）；importer `build_session` 正規化為完整六鍵 provenance——既有 `repo`／`commit`／`path` 之外新增 `commit_source`／`branch`／`dirty`（`dirty` 一律落盤 `"true"`/`"false"` 字串），payload 缺 `commit` 時改以 `_git.git_head(discovered_toplevel)` 補 HEAD 並標記 `commit_source: import-discovery`，兩者皆無才維持 `_unknown`（不寫 `commit_source`，不謊報來源）。
- 新增共用常數 `slice_frontmatter.PROVENANCE_KEYS`，atomizer `pipeline.py` 的 `_split_pass`／`_read_fragment`／`_render_fragment` 與 `slice_frontmatter.render()` 四處由三鍵改六鍵貫通 fragment 與 knowledge slice（`repo`/`commit`/`path` 永遠渲染，其餘三鍵存在且非空才渲染，不補空字串佔位）；`janitor/record_source.py` 同步改為 import 該常數（原本硬編一份重複 tuple），janitor 稽核視角與 slice 落盤內容一致，且與 atomizer 之間仍無循環 import。
- 新增一次性 migration `hippo knowledge backfill-provenance [--dry-run|--apply]`：對既有 knowledge slice，用 `provenance.path` 指到的 archive queue payload（`cwd` + `ended_at`/`timestamp`，缺時回退 note 自身 `captured_at`，容忍未加引號的 YAML datetime）經 `git rev-list --before=<ts>` 近似回填 commit（標記 `commit_source: backfill-approx`）並補 `cites`（沿用 fix 1c 的 `extract_cites`，規則為 differs-or-missing）；`--dry-run`（預設）不寫檔，`--apply` 經 `frontmatter_io.update()` 逐位元冪等；真實 commit（非 `_unknown`）與既有 `commit_source` 一律不覆寫；`provenance.path` 圍籬比照 `recovery.py`，逃逸路徑歸類 `path-escape`，note 不動；只碰 `memory_layer == knowledge`。
- janitor 新增 `check_provenance_commit`（`paulsha_hippo/janitor/lifecycle.yaml` 的 `source_checks.check_provenance_commit`，預設 `false`）：開啟後 `_git.git_commit_exists`（`git cat-file -e <sha>^{commit}`，best-effort；先確認 `toplevel` 可用 `git_toplevel()` 解析為 git 目錄，非 git 目錄一律回 `None` 而非誤判 `False`，避免整個 project 被連坐 decay）接進 `rules.py`（`SourceCommitCheck`，priority 高於 `ttl_expired`）／`scanner.run_scan`（lazy + cache 的 `source_commit_exists` closure，多 root 聚合 any-True/all-False/else-None），dangling／不存在的 provenance commit 視為 `source_invalid`；`dream/cli.py` 維持 `lambda record: None`（dream 情境通常無 checkout repo，判斷不出）；flag 關閉時行為與改動前一致（已用真 git repo 手動驗證）。
- 附帶事實：fix 6（`followups`/`show` 補 `check_boundary`）把 `followups.py`／`show.py`／`cli.py` docstring 內唯一觸發 `policy_consumer_lint` 文字啟發式的字面 `knowledge/` 移除後，`python3 -m paulsha_hippo.lint.policy_consumer_lint paulsha_hippo/` 的靜態文字掃描不再把這三個檔案標記為 memory consumer；三者的 `check_boundary` 義務並未消失，改由測試（而非 lint）鎖住。
- 設定總覽（issue #136 全部新增鍵，供跨碎片查找，皆宣告於 `paulsha_hippo/atomizer/atomizer.yaml`，缺鍵即預設值）：`shortlist.collapse_same_topic`（見 `136-hygiene-supersedes.md`）、`shortlist.read_hint`（見 `136-hygiene-show-agent.md`）、`followups.enabled`（見 `136-hygiene-followups.md`）、`episodic_filter`（見 `136-hygiene-episodic.md`）、`projects.yaml` 頂層 `families:`（見 `136-hygiene-supersedes.md`）；本項另新增 janitor 專屬的 `source_checks.check_provenance_commit`（`paulsha_hippo/janitor/lifecycle.yaml`）。
- 建議 live migration 執行順序（皆先 `--dry-run` 看候選再 `--apply`）：`hippo knowledge backfill-provenance` → `hippo knowledge mark-episodic` → `hippo knowledge link-supersedes --tier auto`（review tier 候選人工勾選後 `--accept`）→ `hippo followups extract`。三支 SessionEnd hook 內容已變更，既有安裝需重新 `hippo upgrade plan/prepare/apply`（wheel＋skill）並 `hippo install hooks`（冪等）套用，再以 `hippo doctor` 確認生效。

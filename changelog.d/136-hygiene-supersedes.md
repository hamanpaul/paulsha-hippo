---
type: feat
---
- Issue #136 fix 2a/2b（shortlist 折疊與 supersedes backfill）：新增 `paulsha_hippo/topic.py` 純函式模組（`canonical_title`／`title_tokens`／`family_key`／`is_same_topic`／`collapse_same_topic`，另有薄別名 `recency_key`），供 shortlist 折疊與 supersedes backfill 共用；`projects.yaml` 頂層新增可選 `families:` 分組，讓跨 project 的同主題筆記可比對（沒設 `families` 時不同專案永遠不成對）；`atomizer/pipeline._canonical_title` 改為 import `topic` 版本，移除重複定義。
- shortlist 折疊：`search()` SELECT 加 `m.captured_at` 並回傳於每筆 hit；`_shortlist_common.build_shortlist_and_record` 依 `flags.collapse_same_topic` 在 claim 前跑 `collapse_same_topic`，被折疊者不進 claim、不算 offered，只在 offered ledger 記 `collapsed` 對照表供稽核；kept 項目輸出順序維持相關度排序（依組內最小原始索引重排），recency 只決定同組存活者，不會把整份 shortlist 依 `captured_at` 重排。
- fix：`collapse_same_topic` 的 recency key 原本用 `str(captured_at or "")` 字典序排序，一個存在但格式錯誤的值（如 `"N/A"`／`"yesterday"`／`"2026/08/21"`）會被誤判成最新、搶下錯的 owner；改以 `datetime.fromisoformat` 解析（容忍尾綴 `Z`、naive／aware 兩種形式，aware 值換算 UTC 後去 tzinfo），解析失敗一律回退 `datetime.min`（視為最舊），同 key 維持 stable sort 保留輸入順序；`load_union_projects_config` 合併路徑對 `families` 透傳也補上測試覆蓋。
- link-supersedes：即時路徑 `_attach_unambiguous_supersedes` 拿掉 `distilled_from` 相等的限制，改用 `topic.canonical_title` 比對 title/atom_title/aliases，並要求前身 `captured_at` 不晚於新 slice（仍要求恰一個無歧義匹配、同 project 才配對）。回填路徑新增 `supersedes_link.py` 與 `hippo knowledge link-supersedes [--dry-run|--apply] [--tier auto|review] [--accept]`：auto tier（同 family＋同名＋雙向唯一）可直接 `--apply`；其餘同主題候選（Jaccard／tags≥2／related 互指／tags+title）只寫報表 `runtime/reports/link-supersedes-<ts>.{jsonl,md}`，人工勾選候選後 `--accept`。跨專案配對只透過 `families:` 開通；dry-run 不動任何 note 與 ledger；配對是嚴格 recency 偏序，無自連無環，重跑為 no-op。
- fix（review round 1）：`captured_at` 分三態——任一端在時間地板（缺值/解不開）不配對；等時戳同 `distilled_from` 視為同場 session 兄弟跳過；等時戳不同 `distilled_from` 進 review（reason `equal-captured_at`，方向以 `slice_id` 字典序決定），永不進 auto。publish 路徑的 captured_at 比較改用新公開的 `topic.recency_key`，修正 `Z`／`+08:00`／YAML round-trip 三種寫法字串序互不可比、合法前身漏連的問題。`related` tier 改認 moc/linker 實際寫出的 `[[<title-slug>--<slice_id>]]` wikilink（舊碼比對從未生效）。decayed 過濾改雙側：已退場的 note 不得宣稱自己取代了還活著的 note。CLI 無 review 候選時不寫報表；`apply_pairs` 保留既有 supersedes 順序後接，不再重排。
- 設定：`shortlist.collapse_same_topic`（`paulsha_hippo/atomizer/atomizer.yaml`，預設 `true`）、`projects.yaml` 頂層 `families:`（optional，同義專案分組，供 shortlist 折疊與 link-supersedes 跨專案配對共用）——完整新增鍵清單見 `136-hygiene-provenance.md`。
- 建議 live migration 執行順序（皆先 `--dry-run` 看候選再 `--apply`）：`hippo knowledge backfill-provenance` → `hippo knowledge mark-episodic` → `hippo knowledge link-supersedes --tier auto`（review tier 候選人工勾選後 `--accept`）→ `hippo followups extract`。三支 SessionEnd hook 內容已變更（見 `136-hygiene-provenance.md`），既有安裝需重新 `hippo upgrade plan/prepare/apply`（wheel＋skill）並 `hippo install hooks`（冪等）套用，再以 `hippo doctor` 確認生效。

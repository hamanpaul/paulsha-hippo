### Added

- Canonical atom titles, collision-resistant rich project directory keys,
  publication commit markers, malformed-inbox quarantine, complete backlog
  health census, run IDs, produced-slice reconciliation, and NAME_MAX-safe
  same-directory frontmatter updates.
- Publication-bound atoms remain hidden from MOC/index until a durable commit
  marker exists; recovery repairs a post-commit processing-ledger gap.
- Doctor smoke probes use the same fixed minimal non-secret environment as
  external agent children; Hippo never forwards manager/shell credential values.

---
type: feat
---
- Issue #136 fix 4（session 狀態句降層 episodic）：新增 `noise.episodic_reason`（獨立於 `classify_noise`，非 deletion-grade）：標題命中 session-handoff/handoff/「狀態」結尾，或 body 中 session 狀態句比例 ≥0.5 時回傳短理由字串。分類器拆成 strong／weak 兩級訊號——strong 措辭（如「尚未 commit」「session 結束時」「本次修改僅限」）一行命中即算；weak（目前狀態／下一步／handoff／status／目前／尚未／待辦）需同一行 ≥2 個「不同 pattern」互相佐證才算（不是重疊 span 數，避免同一 weak pattern 在同一行重複出現被誤算成兩次命中）。1 行 body 只接受 strong 命中，≥2 行才走 ratio 規則；新增 `_episodic_content_lines` 讓本分類器跳過 fenced code block，避免程式碼片段裡的字面文字誤判。
- 兩輪收緊（reviewer 抓到的誤降級案例）：`handoff (status|state|note)` 改為需要冒號／行尾分隔符才算強訊號（`handoff status: pending push` 仍算，`handoff status register` 這類複合名詞不再誤觸發）；weak 清單移除裸字 `status`（英文硬體/韌體敘述常見 `status register`/`status field`，過於通用）。
- `atomizer/pipeline.py` 在 `_attach_unambiguous_supersedes` 之後、`validate()` 之前，依 `config.episodic_filter`（預設 `true`）把命中 `episodic_reason` 的 slice 降層為 `memory_layer: episodic` 並記錄 `episodic_reason`，`validate()` 放寬為接受 `knowledge`/`episodic` 兩種 layer；episodic 筆記仍寫入磁碟，只是被既有的 non-knowledge-layer 排除邏輯擋在 MOC index 之外，不進索引不刪檔。`atomize-knowledge-slice.md` skill prompt 加一條規則，請 LLM 不要把純 session 狀態陳述當知識輸出。
- 新增一次性 migration `hippo knowledge mark-episodic [--dry-run|--apply] [--revert SLICE_ID]`：對既存 ~104 篇 knowledge note 套用同一個 `episodic_reason` 分類器，`--dry-run`（預設）只回報候選，`--apply` 降層 `memory_layer knowledge -> episodic` 並寫 `episodic_reason` + lifecycle `archived` 事件，同時標記 `episodic_demoted_by: mark-episodic`——用以與 Task 13 pipeline 在 publish 時就直接寫成 `episodic` 的 note（欄位形狀完全相同）區分；`--revert` 只精確反向帶這個標記的 note，其餘回報 `not-migrated`（exit 1），不刪檔（MOC index 已依 `memory_layer` 排除）。`frontmatter_io.update` 新增 `remove=` kwarg 供 revert 移除 `episodic_reason`。寫檔成功即計入 `updated`（即使後續 ledger append 失敗，note 仍維持已降層、可 revert，warning 訊息不再誤導成 "Failed to update"）；另加 `skipped_moc` 計數（`-moc.md` 略過筆數）。
- 設定：`episodic_filter`（`paulsha_hippo/atomizer/atomizer.yaml` 頂層鍵，預設 `true`）——控制 pipeline 蒸餾當下的自動降層；`mark-episodic` 是一次性 migration，呼叫時不受此 flag 限制（獨立作用於既存 note）。完整新增鍵清單見 `136-hygiene-provenance.md`。
- 建議 live migration 執行順序（皆先 `--dry-run` 看候選再 `--apply`）：`hippo knowledge backfill-provenance` → `hippo knowledge mark-episodic` → `hippo knowledge link-supersedes --tier auto`（review tier 候選人工勾選後 `--accept`）→ `hippo followups extract`。三支 SessionEnd hook 內容已變更（見 `136-hygiene-provenance.md`），既有安裝需重新 `hippo upgrade plan/prepare/apply`（wheel＋skill）並 `hippo install hooks`（冪等）套用，再以 `hippo doctor` 確認生效。
